# AirPlay Receiver v1.2.1 测试版更新记录

## 修复

- 修复 EXE 的 Windows 命令行转义，并转发 `--minimized`、`--auto-start` 等参数。
- Java 启动器同时支持 `--base-dir 路径` 和 `--base-dir=路径`。
- 修复 UTF-8 系统代码页下原生 AWT 菜单中文显示方框的问题。内置 Java 原字体配置缺少 UTF-8 中文核心字体组合，新增 `jre/conf/fonts/fontconfig.properties` 补齐映射。
- 托盘测试直接测试菜单项，不再注册真实托盘图标，完整构建可以正常退出。
- 源码目录自启动使用内置 `javaw.exe`，不依赖系统 JAR 文件关联；注册表更新失败会恢复选项并提示错误。

## 功能

- 托盘菜单：显示主窗口、启动服务/停止服务、全屏复选项、设置、关于、退出。
- 启停过程中暂时禁用服务动作；全屏状态与服务同步。
- 开机自动启动及登录后自动运行服务选项。
- 中英文菜单及关于信息；英文 README 同步中文版本的文案调整。

## 命名与外观

- 产品中文名统一为「AirPlay 接收器」：窗口标题（原「Java AirPlay 启动器」）、托盘提示、关于信息、安装器名称与快捷方式、原生 EXE 提示及 README 同步更新。
- 应用图标重制：原白色 LOGO 在浅色任务栏/托盘下不易辨认，改为蓝底白图形（material blue 渐变底 + 白色显示器边框与投屏三角），统一应用于窗口/托盘图标（内嵌两个 JAR）、原生启动器 EXE 和安装包。
- 安装器升级时自动删除旧版本遗留的「AirPlay 接收端」桌面与开始菜单快捷方式。

## 配置

```properties
launcher.autoStart.enabled=true
launcher.autoStart.runService=true
```

这两项默认均为 `false`。勾选 GUI 中的开机自动启动选项才会登记 Windows 登录启动项。

## 验证范围

完整构建包含启动器、原生菜单字体、本地控制、退出看门狗、音频、会话切换及打包类的回归测试。EXE 参数解析测试与原生菜单截图检查已纳入标准构建流程；历史遗留的跳过测试入口 `build_patch_skip_test.ps1` 已删除。

安装版与便携版已在目标电脑实测通过（2026-09-06，含升级安装、桌面/托盘图标显示、中文菜单与自启动）。
